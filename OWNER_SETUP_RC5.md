# DIALIX v1.0 RC5 オーナー権限の設定

## 1. GitHub / Vercelへ反映
このZIPの中身を既存の `dialix-beta` リポジトリへ上書きし、GitHub Desktopから Commit / Push してください。

## 2. Edge Functionを更新
Supabase → Edge Functions → `admin-users` → Edit function を開きます。

このZIP内の次のファイルを、既存コードへ全置換して再デプロイしてください。

`supabase/functions/admin-users/index.ts`

## 3. 自分をオーナーに設定
DIALIXへ現在の管理者アカウントでログインし、管理画面 → ユーザー管理を開きます。

オーナーがまだ存在しない場合だけ、画面上部に「自分をオーナーに設定」が表示されます。確認後に実行すると、ログイン中の管理者がオーナーになります。

オーナーは1名のみです。設定後は以下の操作がサーバー側でも拒否されます。

- 降格
- 停止
- 削除
- 他の管理者によるパスワード変更

オーナー本人のパスワードはマイページから変更できます。

## 権限

- オーナー: 全管理機能。保護対象で、他ユーザーから変更・停止・削除不可。
- 管理者: ユーザー追加・編集・停止・削除・パスワード変更。ただしオーナーには操作不可。
- SV: 管理画面・KPI・リスト等を閲覧・運用。ユーザー管理は閲覧のみ。
- オペレーター: 架電・保存・検索・リマインド・マイページ。

## 注意
`profiles.role` にデータベースのCHECK制約があり `owner` を許可していない場合、オーナー設定時にデータベースエラーが表示されます。その場合はSupabaseのエラー画面を共有してください。実際の制約名に合わせた安全なSQLを作成します。
