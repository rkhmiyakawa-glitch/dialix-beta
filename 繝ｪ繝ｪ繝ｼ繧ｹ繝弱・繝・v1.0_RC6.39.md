# DIALIX v1.0.0 RC6.39

## 修正内容
- CSVステータスマッピングの「使用しない（未架電として取込）」を「使用しない」に変更。
- 「使用しない」を選択した行は `customers.status` に `未架電` を保存。
- ステータス列を割り当てない場合も `未架電` として保存。
- `customers.status` の NOT NULL 制約違反を防止。
- RC6.38までの修正内容を維持。
